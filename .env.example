# Admin password for /admin configuration panel (required for admin access)
# Copy to .env.local and set your own password — never commit .env.local
VITE_ADMIN_PASSWORD=your-secure-password-here

# GitHub Pages base path (optional)
# VITE_BASE_PATH=/kuldeep-portfolio/
