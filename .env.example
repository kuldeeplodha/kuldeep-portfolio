# GitHub Pages base path (optional)
# VITE_BASE_PATH=/kuldeep-portfolio/

# V2.2 content backend (FastAPI + Turso, see backend/README.md). Needed to
# sign in to /admin at all (single JWT gate, V2.2 P4) — local dev default
# shown. In `vite dev` / Vitest you can skip this entirely via the "Continue
# without backend (dev only)" button on the /admin sign-in screen.
# VITE_API_BASE_URL=http://localhost:8000
